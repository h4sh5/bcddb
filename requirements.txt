datasketch~=1.5.3

# for experimenting with other hash functions in run_experiments.py
ssdeep
# careful of installing this one, try https://github.com/skiloop/simhash/issues/1
pysimhash

# for web app
flask

# tlsh
python-tlsh~=4.5.0
werkzeug>=3.0.1 # not directly required, pinned by Snyk to avoid a vulnerability
